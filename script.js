const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:3000'); await page.hover('button'); await new Promise(r => setTimeout(r, 1000)); const color = await page.evaluate(() => { const el = document.querySelector('button span'); return window.getComputedStyle(el).color; }); console.log('Color:', color); await browser.close(); })();
